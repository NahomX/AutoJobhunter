# Meshy AI API key — get yours at https://www.meshy.ai/
# Leave unset to run in mock mode (full UI flow, placeholder 3D model)
MESHY_API_KEY=

# Public base URL used to build absolute photo URLs for the Meshy API.
# Change this to your deployed URL in production.
NEXT_PUBLIC_BASE_URL=http://localhost:3000
